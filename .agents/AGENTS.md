# Custom Workspace Rules - Nova Max Hospital

- **DO NOT** deploy the website to production (Firebase Hosting / `firebase deploy`) until the user explicitly reviews the changes and gives authorization (e.g., "okay", "deploy it", "proceed to deploy"). Save all modifications locally and verify with local builds (`npm run build`) first.
